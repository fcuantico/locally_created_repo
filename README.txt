This repo was first created
locally in the PC. 

Then a repo with the same name was 
created in github. Later the locally
created repo was pushed to github as fallows:

…or create a new repository on the command line
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/fcuantico/locally_created_repo.git
git push -u origin main


…or push an existing repository from the command line
git remote add remote_name https://github.com/fcuantico/repo_name.git
git branch -M main
git push -u origin main


